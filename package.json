{
  "name": "prime-video-ad-skipper",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test.mjs",
    "build": "node build.mjs",
    "test:build": "node test-build.mjs",
    "verify": "npm test && npm run build && npm run test:build"
  }
}
